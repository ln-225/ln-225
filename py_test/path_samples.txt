#!/usr/bin/env python
# -*- coding: utf-8 -*-
import os
target_dir='/home/xydevops/samples/'
for root,dirs,files in os.walk(target_dir):
    list_path=root
    list_dir=dirs
    list_file=files
    print(list_path)
    print(list_dir)
    print(list_file)

    for file in files:                  
        print(os.path.join(root,file))
    for dir in dirs:
        print(os.path.join(root, dir))

def list_sys(lists):       #将遍历的元素放进字典
    i=0
    for element in lists:
        if len(lists[i])!=0:
            lists[i]=element
            ++j
    return lists

for root,dirs,files in os.walk(target_dir):  #获取当前路径，所有子目录名，所有子文件名
    print(os.listdir(list_path))
    print(os.listdir(list_dir))
    print(os.listdir(list_file))

    print(dirs)
    print(files)

for i in os.listdir(target_dir):
    print(i)
    if os.path.isdir(target_dir+i):
        target_dir=target_dir+i
        print(os.listdir(target_dir))
        continue    
    elif os.path.isfile(target_dir+i):
        print(os.listdir(target_dir))    

 print(os.listdir(target_dir))
    print(target_dir+i)    拼接子目录

    for i in os.listdir(root):
        print(root[22:])
#判断目标对象是目录还是文件
os.path.isdir(path)
os.path.isfile(path) 
list.append()
os.path.join(path1[, path2[, ...]])    把目录和文件名合成一个路径



